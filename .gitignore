# ignore knexfile as psql config is different on both dev machines
knexfile.js

# sqlite database causes errors when committing to github
app/db/sqlite.db

# ignore tracking usage data config - we want a different one for each user
usage-data-config.json

# ignore extension SCSS because it's dynamically generated
lib/extensions/_extensions.scss
.env
.sass-cache
.DS_Store
.start.pid
.port.tmp
public
node_modules/*
.tmuxp.*
package-lock.json
